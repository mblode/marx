---
"marx-css": patch
---

Give single `<select>` menus a custom dropdown chevron with proper spacing, instead of the cramped native arrow jammed against the edge. Multi/size selects keep their native rendering.
